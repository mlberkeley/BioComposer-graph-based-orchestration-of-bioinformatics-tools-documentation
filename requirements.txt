sphinx>=7
furo>=2024.1
myst-parser>=2
sphinxcontrib-mermaid>=0.9
# NOTE: biocomposer itself is installed separately in the docs workflow with
# --no-deps (its heavy runtime deps are mocked in conf.py via
# autodoc_mock_imports), so it is intentionally NOT listed here.
